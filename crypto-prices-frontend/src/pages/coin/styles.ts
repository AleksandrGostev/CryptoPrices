import styled from "styled-components";

export default styled.div`
  padding: 50px;
  .ant-card-head-title {
    padding: 20px;
    
    .name {
      font-size: 1.6em;
      font-weight: 600;
    }
    .symbol {
      color: gray;
    }
  }
    .swap {
      cursor: pointer;
    }
`;
